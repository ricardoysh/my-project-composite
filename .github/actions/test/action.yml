nome: 'Test'
description: 'Test'

outputs:
  NOME: ${{ step.test.outputs.NOME }}

runs:
  using: composite

  steps:
    - name: test
      id: test
      shell: bash
      run: |
        echo "----- TEST ------"
        echo "NOME=ricardo" >> $GITHUB_OUTPUT