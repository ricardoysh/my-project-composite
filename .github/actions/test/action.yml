nome: 'Test'
description: 'Test'

outputs:
  NOME:
    description: "Nome"
    value: ${{ steps.test.outputs.NOME }}

runs:
  using: composite

  steps:
    - name: test
      id: test
      shell: bash
      run: |
        echo "Setting actor ..."
        echo "AUTOR=ricardo" >> $GITHUB_OUTPUT