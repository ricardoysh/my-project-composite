nome: 'Test'
description: 'Test'

outputs:
  AUTOR:
    description: "Autor"
    value: ${{ steps.test.outputs.AUTOR }}

runs:
  using: composite

  steps:
    - name: test
      id: test
      shell: bash
      run: |
        echo "Setting actor ..."
        echo "AUTOR=ricardo" >> $GITHUB_OUTPUT