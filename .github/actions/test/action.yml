nome: 'Test'
description: 'Test'

outputs:
  NOME:
    description: "Nome"
    value: ${{ steps.test.outputs.NOME }}

runs:
  using: composite

  steps:
    - name: test
      id: test
      shell: bash
      run: |
        echo "----- TEST ------"
        echo "NOME=ricardo" >> $GITHUB_OUTPUT